flask
flask-cors
requests

# ML + embeddings
sentence-transformers
transformers

# CPU-only PyTorch (no CUDA – builds fast)
torch==2.2.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

numpy
faiss-cpu

# Production server
gunicorn

